{
  "name": "@0xdeafcafe/skills-evals",
  "version": "0.1.0",
  "private": true,
  "description": "Eval harness for the 0xdeafcafe-skills plugin. Two tiers: contract tests (no LLM) and pipeline fixtures (real SDK calls, optionally routed through langwatch's AI gateway).",
  "type": "module",
  "scripts": {
    "tier1": "node runners/run-tier1.ts",
    "tier2": "node runners/run-pipeline.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.1",
    "@opentelemetry/context-async-hooks": "^2.7.1",
    "@opentelemetry/sdk-node": "^0.218.0",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "langwatch": "^0.32.0"
  },
  "packageManager": "pnpm@10.15.1"
}
